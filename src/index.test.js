import marvel from './index'

test.skip('should be an array of strings', () => {
  expect(marvel.all).toBe(Array)
})

test.skip('should contain `Wolverine`', () => {

})

test.skip('should return a random item from the marvel.all', () => {

})

test.skip('should return an array of random items if passed a number', () => {

}) 
